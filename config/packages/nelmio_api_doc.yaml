nelmio_api_doc:
    documentation:
        info:
            title: University Timetable
            description: Application for create and manage timetable in some university
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
